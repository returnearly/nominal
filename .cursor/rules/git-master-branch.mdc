---
description: Default git branch is master; author is tom@returnearly.net
alwaysApply: true
---

# Git

Use `master` as the default branch. Never create or push `main`.

This repo is under `~/Projects/re`, so commit as **Tom Schlick `<tom@returnearly.net>`**.
